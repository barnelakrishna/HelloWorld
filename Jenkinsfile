node{
    def biuldNumber = BUILD_NUMBER
    stage("git clone"){
        git url: 'https://github.com/barnelakrishna/HelloWorld.git', branch: 'master'
    }
  stage("print message"){
    echo "this is saample pipeline script feature will update as we required.... master branch"
  }
}
